// Computes the ith fibonacci number.
int fib(int n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int main() {
    int n = (int) scan();
    print((string) fib(n));
    return 0;
}