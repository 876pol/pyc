int main() {
    int i = inputi();
    float f = inputf();
    string s = inputs();
    prints("You inputed " + (string) i);
    prints("You inputed " + (string) f);
    prints("You inputed " + s);
    return 0;
}